
# vdbSetOptions(
#    options(vdbConn = list(
#       vdbName = "vdbTest", # project name
#       vdbPrefix="~/Desktop/vdbtest",
#       defaultStorage="local",
#       mongoHost = "127.0.0.1",
#       mongoName = "",
#       mongoUser = "",
#       mongoPass = ""
#    ))
#    options(vdbDefaultStorage = "local")
# )


# options(vdb_path=NULL)
# 
# # vdb_ppdf: by default, should pdf, swf, png, or postscript 
# # files be generated when vdb.plot is called?
# options(vdb_ppdf=TRUE)
# options(vdb_pswf=TRUE)
# options(vdb_ppng=TRUE)
# options(vdb_pps=FALSE)
# 
# # vdb_remote2local: should commands such as typeset and vdb.plot 
# #      automatically sync the related output with another machine
# #      (such as your local machine)?  This is useful if all work is
# #      being done on a remote machine, but you want to be able to
# #      view plot results immediately
# options(vdb_remote2local=FALSE)
# options(vdb_remote_sync_method="scp") # scp or share
# options(vdb_rmt_local_address=NULL)
# options(vdb_rmt_local_base_path=NULL)
# 
# options(vdb_png_device="png") # either "png" or "CairoPNG" or "cairo"
# 
# # vdb_thumb_height: thumbnail height
# options(vdb_thumb_height=100)
# # vdb_thumb_max_width: maximum width of thumbnails
# #      (for instances when plots have extremely small aspect ratios)
# options(vdb_thumb_max_width=NULL)
# 
# options(vdb_png_res=150)
# options(vdb_png_bg="white")
